//creating the object that holds grid data
function grid(){
	var row = []
	var grid = []
for(var i=0; i < 40; i++){
	row.push(' ')
};
for(var x=0; x < 40; x++){
	grid.push(row)
	} 

	return grid;
}

function render(grid){
	for(var y=0; y < 40; y++)


}
